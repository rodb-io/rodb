$id: https://rodb.io/schema/yaml/inputs/inputs.yaml
$schema: http://json-schema.org/draft-07/schema#
type: array
description: |
  # Inputs

  TODO inputs introduction

  ### Example:
  ```yaml
  TODO
  ```
items:
  anyOf:
    - $ref: ./csv.yaml
      title: CSV
    - $ref: ./json.yaml
      title: JSON
    - $ref: ./xml.yaml
      title: XML
