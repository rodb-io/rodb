# TODO data from stdin? Test reading /dev/stdin
# TODO e2e tests?
# TODO performance analysis
parsers:
  defaultInteger:
    integer:
      ignoreCharacters: ", .€$"
  defaultFloat:
    float:
      decimalSeparator: "."
      ignoreCharacters: ", "
  defaultBoolean:
    boolean:
      trueValues: ["true", "1", "yes", "TRUE", "YES"]
      falseValues: ["false", "0", "no", "FALSE", "NO", ""]
  defaultString:
    string:
      convertFromCharset: ""
sources:
  defaultSource:
    filesystem:
      path: ./
  # TODO S3 source
  # TODO http source
inputs:
  defaultInput:
    csv:
      source: defaultSource
      path: ./dummy.csv
      ignoreFirstRow: true
      delimiter: ","
      columns:
        - name: id
          parser: defaultInteger
        - name: name
          parser: defaultString
        - name: number
          parser: defaultFloat
        - name: bool
          parser: defaultBoolean
  defaultInput2:
    csv:
      source: defaultSource
      path: ./dummy2.csv
      ignoreFirstRow: true
      autodetectColumns: true
      delimiter: ","
  # TODO xml input
  # TODO json input
  # TODO json rows input
  # TODO database source/input?
indexes:
  defaultIndex:
    memoryMap:
      dieOnInputChange: true
      input: defaultInput
      columns:
        - id
        - name
  # TODO stored indexes (bson or msgpack?)
  # TODO bleve index
  # TODO lucene index
services:
  defaultService:
    http:
      # TODO add health-checks
      listen: ":80"
      errorsType: application/json
  # TODO https service
  # TODO authentication
  # TODO websocket service
  # TODO console (stin pipeable?) service
outputs:
  # TODO autogenerated API doc output?
  defaultGraphqlOutput:
    graphql:
      services:
        - defaultService
      endpoint: "/"
      # TODO configuration
  defaultJsonArrayOutput:
    jsonArray:
      input: defaultInput
      services:
        - defaultService
      endpoint: "/dummies"
      limit:
        default: 100
        max: 1000
        param: "limit"
      offset:
        param: "offset"
      search:
        id:
          index: defaultIndex
        name:
          index: defaultIndex
      relationships:
        mySubProperty:
          input: defaultInput
          index: defaultIndex
          isArray: false
          match:
            - parentColumn: id
              childColumn: id
      # TODO sorting
  defaultJsonObjectOutput:
    jsonObject:
      input: defaultInput
      index: defaultIndex
      services:
        - defaultService
      endpoint: "/dummies/?"
      relationships:
        mySubProperty:
          input: defaultInput
          index: defaultIndex
          isArray: false
          match:
            # TODO move the index inside each match and use record.JoinPositionLists for each of the indexes's results
            - parentColumn: id
              childColumn: id
          relationships:
            mySubSubProperty:
              input: defaultInput
              index: defaultIndex
              isArray: true
              match:
                - parentColumn: id
                  childColumn: id
              sort:
                - column: id
                  ascending: true
      parameters:
        # TODO add a limit to the join method, use it here (move the index in each param)
        - column: id
          parser: integer
